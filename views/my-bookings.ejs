<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Bookings | CineReserve</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            mint: {
              50: '#f0fdf4',
              100: '#dcfce7',
              200: '#bbf7d0',
              500: '#10b981',
              600: '#059669',
              900: '#064e3b',
            }
          },
          animation: {
            'ticket-pop': 'ticketPop 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards',
          },
          keyframes: {
            ticketPop: {
              '0%': { opacity: '0', transform: 'scale(0.9) translateY(20px)' },
              '100%': { opacity: '1', transform: 'scale(1) translateY(0)' },
            }
          }
        }
      }
    }
  </script>
</head>

<body class="bg-mint-50 text-mint-900 min-h-screen font-sans pb-20">

  <nav class="p-6 bg-white/80 backdrop-blur-md border-b border-mint-100 sticky top-0 z-50 shadow-sm">
    <div class="max-w-5xl mx-auto flex justify-between items-center">
      <a href="/" class="text-2xl font-black text-mint-900 tracking-tighter">
        CineReserve <span class="text-mint-500">🎬</span>
      </a>
      <a href="/" class="text-sm font-bold text-mint-600 hover:text-mint-900 transition-colors">
        ← Back to Explore
      </a>
    </div>
  </nav>

  <main class="max-w-3xl mx-auto py-16 px-6">
    <header class="mb-12 text-center">
      <h1 class="text-4xl font-black text-mint-900 mb-2">My Ticket Wallet</h1>
      <p class="text-slate-500 font-medium">Your upcoming cinematic adventures are stored here.</p>
    </header>

    <% if (bookings.length === 0) { %>
      <div class="text-center py-20 bg-white rounded-[3rem] border border-dashed border-mint-200">
        <span class="text-6xl block mb-6">🎟️</span>
        <p class="text-slate-400 font-bold text-lg mb-6">No active bookings found.</p>
        <a href="/" class="px-8 py-3 bg-mint-900 text-white rounded-full font-bold hover:bg-black transition-all">Start Booking</a>
      </div>
    <% } %>

    <div class="space-y-8">
      <% bookings.forEach((b, index) => { %>
        <div class="animate-ticket-pop opacity-0 group" style="animation-delay: <%= index * 0.1 %>s;">
          <div class="bg-white rounded-[2rem] border border-mint-100 shadow-xl shadow-mint-200/40 overflow-hidden flex flex-col md:flex-row">
            
            <div class="bg-mint-900 text-white p-8 md:w-1/3 flex flex-col justify-center items-center text-center border-b md:border-b-0 md:border-r border-dashed border-mint-700/50 relative">
              <span class="text-5xl mb-4 group-hover:rotate-12 transition-transform duration-500">🎞️</span>
              <h3 class="text-xl font-black leading-tight"><%= b.show.movie.title %></h3>
              <p class="text-mint-400 text-[10px] font-black uppercase tracking-[0.2em] mt-2">Verified Ticket</p>
              
              <div class="hidden md:block absolute -top-4 -right-4 w-8 h-8 bg-mint-50 rounded-full border border-mint-100"></div>
              <div class="hidden md:block absolute -bottom-4 -right-4 w-8 h-8 bg-mint-50 rounded-full border border-mint-100"></div>
            </div>

            <div class="p-8 flex-grow grid grid-cols-2 gap-y-6 gap-x-4">
              <div>
                <p class="text-[10px] text-slate-400 font-black uppercase tracking-widest mb-1">Cinema Venue</p>
                <p class="text-sm font-bold text-mint-900"><%= b.show.theatre.name %></p>
              </div>
              <div>
                <p class="text-[10px] text-slate-400 font-black uppercase tracking-widest mb-1">Auditorium</p>
                <p class="text-sm font-bold text-mint-900"><%= b.show.screen.name %></p>
              </div>
              <div>
                <p class="text-[10px] text-slate-400 font-black uppercase tracking-widest mb-1">Showtime</p>
                <p class="text-sm font-bold text-mint-900">
                  <%= new Date(b.show.showTime).toLocaleDateString([], { month: 'short', day: 'numeric' }) %> • 
                  <%= new Date(b.show.showTime).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) %>
                </p>
              </div>
              <div>
                <p class="text-[10px] text-slate-400 font-black uppercase tracking-widest mb-1">Seats Allocated</p>
                <div class="flex flex-wrap gap-1 mt-1">
                  <% b.seats.forEach(seat => { %>
                    <span class="px-2 py-0.5 bg-mint-50 text-mint-600 text-[10px] font-black rounded border border-mint-100"><%= seat %></span>
                  <% }) %>
                </div>
              </div>

              <div class="col-span-2 border-t border-mint-50 pt-6 flex justify-between items-center">
                <div>
                  <p class="text-[10px] text-slate-400 font-black uppercase tracking-widest">Total Amount Paid</p>
                  <p class="text-2xl font-black text-mint-900">₹<%= b.totalAmount %></p>
                </div>
                <div class="text-right">
                   <div class="inline-block p-2 bg-mint-50 rounded-lg">
                      <img src="https://api.qrserver.com/v1/create-qr-code/?size=50x50&data=<%= b._id %>" alt="QR" class="opacity-80">
                   </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </main>

  <footer class="text-center py-10 opacity-30">
    <p class="text-[10px] font-black uppercase tracking-[0.5em]">CineReserve Vault &bull; 2026</p>
  </footer>

</body>
</html>