<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin: Manage Theatres | CineReserve</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            mint: {
              50: '#f0fdf4',
              100: '#dcfce7',
              200: '#bbf7d0',
              500: '#10b981',
              600: '#059669',
              900: '#064e3b',
            }
          }
        }
      }
    }
  </script>
</head>

<body class="bg-mint-50 text-mint-900 min-h-screen font-sans selection:bg-mint-200">

  <nav class="p-6 bg-white border-b border-mint-100 flex justify-between items-center sticky top-0 z-50 shadow-sm">
    <div class="max-w-5xl w-full mx-auto flex justify-between items-center">
      <a href="/" class="text-2xl font-black text-mint-900 tracking-tighter flex items-center gap-2">
        CineReserve 🎬 
        <span class="text-[10px] uppercase bg-mint-900 text-white px-2 py-1 rounded-md tracking-widest ml-2">Admin</span>
      </a>
      <div class="flex items-center gap-6">
        <a href="/admin" class="text-sm font-bold text-mint-600 hover:text-mint-900 transition-colors">Dashboard</a>
        <a href="/" class="text-sm font-bold text-slate-400 hover:text-mint-900 transition-colors">← Exit</a>
      </div>
    </div>
  </nav>

  <main class="max-w-5xl mx-auto py-16 px-6">
    
    <header class="mb-10 text-center">
      <h2 class="text-4xl font-black text-mint-900 mb-2">Cinema Venues</h2>
      <p class="text-slate-500 font-medium">Register physical theatre locations and map them to cities</p>
    </header>

    <div class="grid grid-cols-1 lg:grid-cols-12 gap-10">
      
      <div class="lg:col-span-5">
        <div class="bg-white rounded-[2.5rem] border border-mint-100 shadow-2xl shadow-mint-200/40 p-8 sticky top-28">
          <h3 class="text-xl font-black text-mint-900 mb-6">New Venue</h3>
          
          <form action="/api/theatres" method="POST" class="space-y-6">
            <div>
              <label class="block text-xs font-black uppercase text-mint-900/40 mb-3 ml-1 tracking-widest">Theatre Name</label>
              <input type="text" name="name" placeholder="e.g. Royal Cinema" required 
                class="w-full bg-mint-50 border border-mint-100 rounded-2xl p-4 text-mint-900 font-bold focus:ring-4 focus:ring-mint-200 focus:border-mint-500 outline-none transition-all">
            </div>

            <div>
              <label class="block text-xs font-black uppercase text-mint-900/40 mb-3 ml-1 tracking-widest">City Hub</label>
              <select name="city" required class="w-full bg-mint-50 border border-mint-100 rounded-2xl p-4 text-mint-900 font-bold focus:ring-4 focus:ring-mint-200 focus:border-mint-500 outline-none appearance-none cursor-pointer">
                <option value="">Select a city...</option>
                <% cities.forEach(city => { %>
                  <option value="<%= city._id %>"><%= city.name %></option>
                <% }) %>
              </select>
            </div>

            <div>
              <label class="block text-xs font-black uppercase text-mint-900/40 mb-3 ml-1 tracking-widest">Street Address</label>
              <input type="text" name="address" placeholder="123 Cinema Lane..." required 
                class="w-full bg-mint-50 border border-mint-100 rounded-2xl p-4 text-mint-900 font-bold focus:ring-4 focus:ring-mint-200 focus:border-mint-500 outline-none transition-all">
            </div>

            <button type="submit" class="w-full bg-mint-900 text-white text-lg font-black py-4 rounded-2xl shadow-xl shadow-mint-900/20 hover:bg-black transition-all transform active:scale-[0.98]">
              Add Theatre
            </button>
          </form>
        </div>
      </div>

      <div class="lg:col-span-7">
        <h3 class="text-xl font-black text-mint-900 mb-6">Established Venues</h3>
        
        <div class="bg-white rounded-[2.5rem] border border-mint-100 overflow-hidden shadow-xl shadow-mint-200/20">
          <table class="w-full text-left">
            <thead class="bg-mint-50 text-mint-900/40 text-[10px] uppercase font-black tracking-widest">
              <tr>
                <th class="p-5">Theatre</th>
                <th class="p-5">Hub</th>
                <th class="p-5">Address</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-mint-50">
              <% theatres.forEach(theatre => { %>
                <tr class="hover:bg-mint-50 transition-colors group">
                  <td class="p-5">
                    <p class="text-mint-900 font-black group-hover:text-mint-500 transition-colors"><%= theatre.name %></p>
                  </td>
                  <td class="p-5 text-sm">
                    <span class="px-3 py-1 bg-mint-100 text-mint-600 rounded-full font-bold text-[10px] uppercase">
                      <%= theatre.city.name %>
                    </span>
                  </td>
                  <td class="p-5 text-xs text-slate-400 font-medium">
                    <%= theatre.address %>
                  </td>
                </tr>
              <% }) %>
              <% if (theatres.length === 0) { %>
                <tr>
                  <td colspan="3" class="p-10 text-center text-slate-300 font-bold italic">No theatres added to the network.</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>

    </div>
  </main>

  <footer class="py-12 text-center text-mint-200 text-[10px] uppercase font-black tracking-widest">
    CineReserve Admin Console &bull; 2026
  </footer>

</body>
</html>